import 'package:flutter/material.dart';

var searchBarShadow = const BoxShadow(
    offset: Offset(0, 1),
    blurRadius: 3,
    spreadRadius: 0.5,
    color: Color.fromRGBO(64, 60, 3, 0.3));

var containerShadow = const BoxShadow(
    offset: Offset(0, 0),
    blurRadius: 3,
    spreadRadius: 0,
    color: Color.fromRGBO(64, 60, 3, 0.3));
